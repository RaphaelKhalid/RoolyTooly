# Morning digest — 2026-08-30

_Covering everything since 2026-08-29T00:05:28Z (UTC)._

## What we learned
- **M23 – delivering something different from what was described** (60 new item(s)): “record_observation summary is overlong. The new docstring summary exceeds 80 characters and ends mid-thought rather than as a…” — https://github.com/RaphaelKhalid/RoolyTooly/pull/4#discussion_r3887379015
- **NEW:qodo-unclassified** (23 new item(s)): “Failed commands count as execution. Setup and program execution are inferred only from submitted command strings, without…” — https://github.com/RaphaelKhalid/RoolyTooly/pull/2#discussion_r3887279899
- **M14 – a fix that breaks something else** (8 new item(s)): “Errored candidates pass regression. candidate_passes applies all() after filtering out errored candidate results, so it is true…” — https://github.com/RaphaelKhalid/RoolyTooly/pull/2#discussion_r3887279913
- **M05 – trusting a printed summary instead of checking the real file** (7 new item(s)): “Revert reports nonexistent success. revert_lesson appends a status and returns success without checking that the lesson exists…” — https://github.com/RaphaelKhalid/RoolyTooly/pull/2#discussion_r3887279937
- **M07 – deleting the evidence before anyone could check it** (4 new item(s)): “Corrupt record disables ledger. _replay parses every JSONL line and indexes kind without handling malformed or partially written…” — https://github.com/RaphaelKhalid/RoolyTooly/pull/2#discussion_r3887279933
- ...plus 12 more item(s) across 5 other families (see ledger/ledger.jsonl).

## Rules added / merged / retired
No rule changes since 2026-08-29 00:05 UTC.

## Bugs caught in review
- 114 finding(s) the code reviewer caught in our own pull requests, imported as observations (from `ledger/ledger.jsonl`).
- Grouped by kind of mistake — M23 – delivering something different from what was described: 60; NEW:qodo-unclassified: 23; M14 – a fix that breaks something else: 8; M05 – trusting a printed summary instead of checking the real file: 7; M07 – deleting the evidence before anyone could check it: 4; M09 – insisting on an answer that was already corrected: 4; M11 – misreading what actually happened: 4; M13 – mixing different kinds of numbers together: 2; M03 – declaring victory on a side effect instead of the real goal: 1; M21 – dropping a promise made earlier in the conversation: 1.
- 8 candidate rule(s) suggested, none of them live yet — each still has to pass testing and your approval (`qodo_rule_proposals.json`):
    - M23 – delivering something different from what was described (60 findings): Ensure what is displayed or reported matches the underlying data exactly, including rows or cases the presentation layer would otherwise drop.
    - UNCLASSIFIED (23 findings): Classify this finding into a mistake family by hand before drafting a rule; an unclassified finding cannot produce a transferable invariant.
    - M14 – a fix that breaks something else (8 findings): Run the whole existing test suite, not only the new test, and add a regression test that fails before the fix and passes after it.
    - M05 – trusting a printed summary instead of checking the real file (7 findings): After any command or write, open the resulting artifact and confirm it is non-empty and well-formed before treating the step as successful.
    - M07 – deleting the evidence before anyone could check it (4 findings): Preserve every artifact a check or a human may still need to read; never delete, overwrite, or regenerate evidence before it has been evaluated.

## Did it help?
No data (need at least two results/timeline_*.json files; found 0).

## Open problems
No data (no results/timeline_*.json files yet).
