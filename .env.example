# Copy to .env (gitignored). Never commit real values.
DAYTONA_API_KEY=            # Daytona key with Sandboxes + Snapshots(write); used by bench/run.py cleanup and scripts/purge_sandboxes.py
BRIGHTDATA_API_KEY=         # Bright Data MCP token (Workflow C, mistake mining)
TRUEFORGE_URL=http://localhost:8790
# OpenAI key and GitHub PAT are entered in TrueForge -> Settings (Models / Connectors), not here.
